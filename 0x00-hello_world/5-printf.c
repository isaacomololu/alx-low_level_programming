#include <stdio.h>
/**
 * main-entry point for the program.
 *
 * Return:Zero if no errors, return non zero if error
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}

